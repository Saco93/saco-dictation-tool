# Component Inventory - sttctl (Exhaustive)

## CLI Module

- `src/main.rs`
  - argument model (`Args`)
  - command enum (`Commands`)
  - socket path resolution (`--socket-path` or config-derived)
  - request dispatch and response rendering
